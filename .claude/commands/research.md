Use the `official-docs-researcher` subagent for this topic:

$ARGUMENTS

The subagent must:
1. read the latest official docs from the creator first,
2. identify recommended and discouraged patterns,
3. inspect the closest existing NotesApp code paths,
4. recommend the most repo-coherent high-quality approach,
5. explicitly state confidence.

Do not jump to final implementation until the research summary is complete.