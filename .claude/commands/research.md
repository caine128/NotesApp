Use the `official-docs-researcher` subagent for this topic:

$ARGUMENTS

The subagent must:
1. read creator-owned official docs first,
2. use Context7 or similar current-doc tooling when available,
3. identify recommended and discouraged patterns,
4. inspect the closest existing NotesApp code paths,
5. recommend the most repo-coherent high-quality approach,
6. explicitly state confidence.

Do not jump to final implementation until the research summary is complete.
Keep the summary concise and distilled.